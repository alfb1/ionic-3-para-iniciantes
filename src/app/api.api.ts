import {environment} from '../environments/environment';

export const URL     = environment.url;
export const API_KEY = environment.api_key;
export const CONFIG  = environment.config;

export const URL_CARTOLA = environment.url_cartola;

